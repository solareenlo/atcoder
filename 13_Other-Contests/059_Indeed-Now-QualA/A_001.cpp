#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(false);

    string a, b;
    cin >> a >> b;
    int n = a.size();
    int m = b.size();
    cout << n * m << '\n';
    return 0;
}
