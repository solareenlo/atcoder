#include <iostream>
using namespace std;

int main(void) {
    cin.tie(0);
    ios::sync_with_stdio(false);

    int a, b, c;
    cin >> a >> b >> c;
    string str;
    cin >> str;
    cout << a + b + c << ' ' << str << '\n';
    return 0;
}
