#include <bits/stdc++.h>
#define REP(i, n) for (int i = 0; i < (n); i++)
using namespace std;
const int INF = 0x7ffffff;
int R, G, B;

int main() {
	cin >> R >> G >> B;
	int N = R + G + B;
	int dp[910];
	memset(dp, '*', sizeof(dp));
	REP(i, 2000) for (int j = N - 1; j >= 0; j--) {
	}
	return 0;
}
