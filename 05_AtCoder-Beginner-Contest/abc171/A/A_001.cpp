#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(false);

    char c;
    cin >> c;
    if (c >= 'A' && c <= 'Z') cout << 'A' << '\n';
    else cout << 'a' << '\n';
    return 0;
}
