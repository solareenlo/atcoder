#include <iostream>
#include <map>
#include <set>
#include <vector>
#define REP(i, n) for (int i = 0; i < (n); i++)
using namespace std;


void solve(long long L){

}

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);
    long long L;
    scanf("%lld",&L);
    solve(L);
    return 0;
}
