#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(false);

    string s; cin >> s;
    if (s.find("AC") != std::string::npos) cout << "Yes" << '\n';
    else cout << "No" << '\n';
    return 0;
}
