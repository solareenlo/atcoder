#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    cin.tie(0)->sync_with_stdio(false);

    string s; cin >> s;
    for (int i = 0; i < 3; i++)
        cout << s[i];
    cout << '\n';
    return 0;
}
